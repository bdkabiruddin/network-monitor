{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "network-monitor",
  "version": "0.0.1",
  "identifier": "bd.kabir.NetworkMonitor",
  "build": {
    "frontendDist": "../frontend"
  },
  "app": {
    "withGlobalTauri": true,
    "windows": [
      {
        "title": "Network Monitor",
        "width": 1300,
        "height": 900,
        "minWidth": 900,
        "minHeight": 600,
        "resizable": true,
        "fullscreen": false
      }
    ],
    "security": {
      "csp": null
    }
  },
  "bundle": {
    "active": true,
    "targets": ["deb", "appimage"],
    "category": "Utility",
    "shortDescription": "Live network bandwidth, connections, and history dashboard",
    "longDescription": "Network Monitor shows live download/upload bandwidth, latency, active TCP/UDP connections, per-process network activity, Wi-Fi analysis, firewall/VPN/interfaces detail, and long-term traffic history, backed by a local SQLite database. Runs in the background with a system tray icon; closing the window hides it instead of quitting.",
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/128x128@2x.png",
      "icons/icon.icns",
      "icons/icon.ico"
    ],
    "linux": {
      "deb": {
        "depends": ["iproute2", "iputils-ping"],
        "recommends": ["network-manager", "nethogs", "policykit-1"]
      }
    },
    "android": {
      "debugApplicationIdSuffix": ".debug"
    }
  }
}
